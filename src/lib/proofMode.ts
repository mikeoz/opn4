/**
 * Proof Mode — Alpha testing flag.
 *
 * When enabled:
 *  • All toasts are persistent (no auto-dismiss).
 *  • Toasts that display an ID include Copy + Dismiss actions.
 *  • IDs are always rendered in-page via ResultPanel with copy icons.
 *
 * Set to `false` for production UX.
 */
export const PROOF_MODE = true;
